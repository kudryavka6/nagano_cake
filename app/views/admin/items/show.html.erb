<h3>商品詳細</h3>
 <% if @item.image.attached? %>
    <%= image_tag item.image, size: "100x100" %>
　<% else %>
    <%= image_tag 'no_image', size: "100x100" %>
 <% end %>

 <h4>商品名</h4>
 <%= @item.name %>

 <h4>商品説明</h4>
 <%= @item.introduction %>

 <h4>ジャンル</h4>

 <h4>税込価格</h4>
 <h4>税抜価格</h4>
 <%= @item.price %>円

 <h4>販売ステータス</h4>
 <%= @item.is_active %>
 <br><%= link_to "編集する" , admin_item_path(@item) %></br>

